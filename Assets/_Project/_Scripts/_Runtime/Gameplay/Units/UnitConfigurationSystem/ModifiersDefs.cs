using UnityEngine;

namespace ACT.Runtime.Gameplay.Units.UnitConfigurationSystem
{
    public enum ShapeModifierType { Cube, Sphere }

    public enum SizeModifierType { Big,  Small }

    public enum ColorModifierType { Red, Green, Blue }
}
